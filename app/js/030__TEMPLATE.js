blockslack.template = (function(){
    
    // privates:
    // var foo = ...
    // var bar = ...

    // initialization:
    // (don't depend on other packages, order of package initialization is not guaranteed)
    // foo = 1;
    // bar = 2;

    return {

        // publics:
        // someFunc: function() { return "hello world"; },
        // SomeConst: 42,

    };

})();
